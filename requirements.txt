# Core dependencies
python-dotenv==1.0.1  # For loading environment variables
PyPDF2==3.0.1         # For PDF text extraction
pandas==2.2.2         # For handling tabular data (e.g., tables from PDFs)
numpy==1.26.4         # For numerical operations
requests==2.31.0      # For HTTP requests (e.g., Azure blob downloads)

# Embedding and vector store
sentence-transformers==3.0.1  # For generating embeddings
faiss-cpu==1.8.0             # FAISS for local vector storage
pinecone-client==5.0.1       # Pinecone for cloud vector storage
pymilvus==2.4.4              # Milvus for vector storage

# Retrieval and reranking
rank_bm25==0.2.2             # For BM25 reranking
scikit-learn==1.5.1          # For MMR reranking and evaluation metrics

# LLM interface
openai==1.35.10              # For OpenAI API (LLM queries)
python-docx==1.1.2           # For rendering DOCX output

# Logging and utilities
logging==0.5.1.2             # Python standard logging (included in Python)
tqdm==4.66.4                 # For progress bars

# Jupyter notebooks
jupyter==1.0.0               # For running notebooks
nbformat==5.10.4             # For handling .ipynb files

# Azure integration
azure-storage-blob==12.20.0  # For Azure blob storage access 
